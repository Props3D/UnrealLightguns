# Shell scripts must keep LF line endings: with Git's default autocrlf on Windows they would be checked out
# with CRLF, and bash then fails with "set: pipefail: invalid option name".
*.sh text eol=lf
*.ps1 text eol=crlf
